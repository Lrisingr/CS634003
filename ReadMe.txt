For all the Microsoft products in amazon file, find out if there is any outlier wrt price.
For all the game related products in googleproducts.csv, present a histogram, where X-axis is the manufacturer name and Y-axis is the no of products by the manufacturer.
There are products in these two files who have different name and description in these two files but referring to the same entity. Give entity resolution algorithm with appropriate pseudo-code and description.
Present a csv file as the output of your algorithm which contains one to one mapping between the ids of the products (one from amazon and the other from google), as they are identified as the same entity by your algorithm.
